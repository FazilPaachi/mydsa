[build-system]
require = ["setuptools>61.0"]
build-backend = "setuptools.build_meta"

[projects]
name = "mydsa"
version = "0.0.1"
description = "ustilities for testing DSA solutions"
readme = "README.md"

authors = [ {name= "Muhammed Fazil C"} ]

keywords = ["python", "dsa", "tests", "testing", "algorithms"]

license = { text = "MIT" }
