[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mydsa"
version = "0.1.0"
description = "DSA testing utilities"
readme = "README.md"
requires-python = ">=3.8"

authors = [
  { name = "Muhammed Fazil C" }
]